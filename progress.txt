2026-03-24 - Task completed: Define the initial OpenAPI 3.0 contract for MVP endpoints.
- Added `openapi.yaml` with paths for `/health`, `/players`, `/matches`, and `/leaderboard`.
- Added component schemas for players, matches (including optional second player per side), leaderboard entries, and standard response/error payloads.

2026-03-24 - Task completed: Scaffold the Go API server with Echo and OpenAPI-driven wiring.
- Added initial Go backend structure (`go.mod`, `cmd/server/main.go`, `internal/api` package, `Makefile`).
- Added `oapi-codegen` configuration and `go generate` hook to generate Echo server/types from `openapi.yaml`.
- Implemented in-memory handlers for `/health`, `/players`, `/matches`, and `/leaderboard` following the OpenAPI contract.

2026-03-24 - Task completed: Replace in-memory API storage with SQLite persistence.
- Added SQLite-backed storage for players and matches, including schema initialization on startup.
- Updated the API handlers to read/write players, matches, and leaderboard calculations from the database.
- Added startup configuration for an environment-driven DB path (`SQLITE_PATH`, default `./data/foosball.db`) and included the SQLite driver dependency.

2026-03-24 - Task completed: Scaffold the SvelteKit SPA frontend with generated API client and TanStack Query.
- Created a new `frontend/` SvelteKit TypeScript app and added TanStack Query for server state handling.
- Added `@hey-api/openapi-ts` generation config/scripts and generated a typed client from `openapi.yaml` into `frontend/src/lib/api`.
- Wired `QueryClientProvider` into the root layout, configured API client base URL defaults, and implemented an MVP dashboard page that fetches players, matches, and leaderboard data with clear loading/empty/error states.

2026-03-24 - Task completed: Implement MVP dashboard write flows for players and matches.
- Updated `frontend/src/routes/+page.svelte` to add an "Add player" form and a "Log match" form using the generated OpenAPI client and TanStack Query mutations.
- Added client-side validation and user feedback for form errors/success, including duplicate-player prevention within a match.
- Added query invalidation after successful mutations so players, matches, and leaderboard data refresh immediately.

2026-03-24 - Task completed: Package API + web UI into a single Docker image with embedded frontend assets.
- Added a multi-stage root `Dockerfile` that builds the SvelteKit app, copies the static bundle into `cmd/server/web/dist`, and compiles one Go server binary that serves API + SPA.
- Updated SvelteKit to `@sveltejs/adapter-static` (SPA fallback `200.html`) and added client-only layout settings for static output.
- Added Echo static/fallback routing for embedded assets and documented `docker build`/`docker run` usage with persisted SQLite via `/data` volume.

2026-03-24 - Task completed: Migrate backend from Echo v4 to Echo v5.
- Upgraded Go dependency to `github.com/labstack/echo/v5` (`v5.0.4`) and updated imports/signatures from `echo.Context` to `*echo.Context`.
- Updated middleware usage for Echo v5 by replacing removed v4 request logging middleware with `middleware.RequestLogger()`.
- Adjusted startup/bootstrapping code for v5 API changes and revalidated the project with `go test ./...` and `npm --prefix frontend run check`.

2026-03-24 - Task completed: Refresh dashboard UI for a cleaner, user-friendly MVP experience.
- Redesigned `frontend/src/routes/+page.svelte` with a card-based, responsive layout and stronger visual hierarchy across create/log/view workflows.
- Improved usability with clearer helper text, polished form/status messaging, and more legible player/match/leaderboard presentation.
- Verified all changes with `go test ./...` and `npm --prefix frontend run check` (both passing).

2026-03-24 - Task completed: Fix Svelte 5 non-reactive dashboard form state warnings.
- Updated mutable local form/message fields in `frontend/src/routes/+page.svelte` to use `$state(...)` so updates correctly trigger UI reactivity in runes mode.
- Replaced deprecated `on:submit` form handlers with `onsubmit` to match Svelte 5 event syntax.
- Verified all changes with `go test ./...` and `npm --prefix frontend run check` (0 errors, 0 warnings).
